import React from 'react';
import Grid from "@mui/material/Grid";

const UserProfileSidebar = ({email,phoneNo}) => {
    return (
        <Grid container spacing={3.75}>
            <Grid item xs={12} md={6} lg={12}>
        </Grid>
           
            
        </Grid>
    );
};

export default UserProfileSidebar;
